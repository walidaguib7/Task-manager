import TasksList from "@/components/Tasks/ShowTasks/TasksList";

const TasksListPage = () => {
  return (
    <div className="pt-10   px-24 max-sm:px-8">
      <TasksList />
    </div>
  );
};

export default TasksListPage;
